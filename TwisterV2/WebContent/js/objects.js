

(function() {
    if (typeof Message !== "undefined") {
    return;
  	}

	this.Message = function(m) {
    this.date=obj["date"];
    this.id = obj["id"];
    this.login = obj["user"][0];
    this.id_user = obj["user"][1];
    this.text = obj["text"];
    this.language= obj["lg"];
    this.nb_like =obj["nb_likes"];
    this.nb_comments = obj["nb_comments"];
  }
	TFillJs.prototype.getHtml = function() {
		return this.getHtml();
  	}
  function getHtml(a) {
     p = {};
     p[]
      return ;
  }

  });

(function() {
    if (typeof Messages !== "undefined") {
    return;
  	}

	this.Messages = function(m) {
    this.m=m["response"]["messages"];
  }
	TFillJs.prototype.getHtml = function() {
		return this.getHtml();
  	}
  function getHtml(a) {
      html = "";
      for (var i = 0; i < Things.length; i++) {
      	Things[i]
      }
  }

  });

