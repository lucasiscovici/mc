package db;

import java.net.UnknownHostException;

import util.Parameters;
import util.Usefull;

public class db_Post_Helper {
	public static String My_Table = Tables.Post;
	public static String date = "date";
	public static String text = "text";
	public static String id_user = "id_user";
	
	public static boolean insertPost(Parameters p) throws UnknownHostException {
		p.AddParam("date", Usefull.currentDate());
		if (db_Helper.insertMongoOK(My_Table, p.PS("text", "id_user"))) {
			
		}
		
		
		
		
		
		return true;
	}
}
